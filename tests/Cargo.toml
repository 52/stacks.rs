[package]
name = "stacks_tests"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["Max Karou <maxkarou@protonmail.com>"]
repository = "https://github.com/52/stacks.rs"
publish = false

[dependencies]
stacks-rs = { path = "../stacks", version = "*" }
