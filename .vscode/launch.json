{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug unit tests in library 'stacks'",
      "cargo": {
        "args": ["test", "--no-run", "--lib", "--package=stacks"],
        "filter": {
          "name": "stacks",
          "kind": "lib"
        }
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
